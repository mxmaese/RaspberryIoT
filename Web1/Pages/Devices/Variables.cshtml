@page
@using Entities
@model Web.Pages.Devices.VariablesModel
@{
    ViewData["Title"] = "Variables";
    ViewData["ConteinsMain"] = false;
}
@section Styles {
    <link rel="stylesheet" href="~/css/Devices/Devices.css" asp-append-version="true" />
}
<main role="main" class="device-page" style="background:none">
    <h2>Actuadores Disponibles</h2>
    <div class="action-buttons">
        <button class="action-btn btn-create">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <line x1="12" y1="5" x2="12" y2="19"></line>
                <line x1="5" y1="12" x2="19" y2="12"></line>
            </svg>
            Crear Nuevo
        </button>
    </div>
    <div class="device-grid">

        @{
            var Variables = ViewData["Variables"] as List<Entities.Variable> ?? default;
            if (Variables == default)
            {
                <h1>No tienes aun ninguna variable</h1>
            }
            else
            {
                <div class="device-card" style=" background: gainsboro; padding-top:0.75rem; padding-bottom:0.75rem">
                    <div class="variable-info">
                        <span class="device-id">Id</span>
                        <h3 class="device-name">Nombre</h3>
                        <div class="device-value">Estado</div>
                        <span class="device-value">Valor</span>
                        <span class="device-value">Es dinamico</span>
                        <span class="device-value">Formula</span>
                    </div>
                </div>
                foreach (var Variable in Variables)
                {
                    <div class="device-card">
                        <div class="variable-info">
                            <span class="device-id">@Variable.VariableId</span>
                            <h3 class="device-name">@Variable.Name</h3>
                            <div class="device-status status-active"></div>
                            <span class="device-value">@Variable.Value</span>

                            <span class="device-value">@Variable.IsDynamic</span>
                            <span class="device-value">@Variable.Formula</span>

                            <button class="device-action-btn btn-modify">
                                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path>
                                    <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path>
                                </svg>
                            </button>
                            <button class="device-action-btn btn-delete">
                                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                    <polyline points="3 6 5 6 21 6"></polyline>
                                    <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path>
                                </svg>
                            </button>
                        </div>
                    </div>
                }
            }
        }
    </div>
</main>